// Define a namespace named Subjects to organize related code together
namespace Subjects {
  // Define an interface named Teacher within the Subjects namespace
  export interface Teacher {
    firstName: string; // Teacher's first name
    lastName: string;  // Teacher's last name
  }
}
